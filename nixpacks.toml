[phases.setup]
nixPkgs = ["ffmpeg", "curl"]

[phases.install]
cmds = [
  "npm ci",
  "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /app/yt-dlp",
  "chmod a+rx /app/yt-dlp"
]

[start]
cmd = "node server.js"
